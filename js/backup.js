function backup(){const data={version:2,exportedAt:new Date().toISOString(),settings:settings(),chat:chats(),saved:saved(),progress:progress()};const blob=new Blob([JSON.stringify(data,null,2)],{type:"application/json"}),a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="mithra-backup.json";a.click();URL.revokeObjectURL(a.href);toast("Backup exported")}
document.addEventListener("DOMContentLoaded",()=>{document.getElementById("export")?.addEventListener("click",backup);document.getElementById("import")?.addEventListener("change",e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{const d=JSON.parse(r.result);if(d.settings)set(K.settings,d.settings);if(d.chat)set(K.chat,d.chat);if(d.saved)set(K.saved,d.saved);if(d.progress)set(K.progress,d.progress);toast("Backup restored");setTimeout(()=>location.reload(),500)}catch{toast("Invalid backup file")}};r.readAsText(f)})})