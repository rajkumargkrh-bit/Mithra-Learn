document.addEventListener("DOMContentLoaded",()=>{const s=getSettings();const theme=document.getElementById("themeSelect"),save=document.getElementById("saveChat"),typing=document.getElementById("typing");theme.value=s.theme;save.checked=s.saveChat;typing.checked=s.typing;
theme.addEventListener("change",()=>{s.theme=theme.value;saveSettings(s);applyTheme(s.theme);toast("Theme updated")});save.addEventListener("change",()=>{s.saveChat=save.checked;saveSettings(s);toast("Chat setting updated")});typing.addEventListener("change",()=>{s.typing=typing.checked;saveSettings(s);toast("Typing setting updated")});
document.getElementById("clearHistory").addEventListener("click",()=>{saveChat([]);toast("Chat history cleared")});document.getElementById("resetApp").addEventListener("click",()=>{if(confirm("Reset Mithra and remove local data?")){clearAllData();location.reload()}})})