document.addEventListener("DOMContentLoaded",()=>{const box=document.getElementById("recent");if(!box)return;const q=chats().filter(x=>x.role==="user").slice(-5).reverse();if(q.length)box.innerHTML=q.map(x=>`<div class="recent-item">${safe(x.content)}</div>`).join("");function safe(s){return s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}});