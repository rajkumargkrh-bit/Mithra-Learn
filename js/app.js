document.addEventListener("DOMContentLoaded",()=>{const list=document.getElementById("recentList");if(!list)return;const chat=getChat();const userMsgs=chat.filter(x=>x.role==="user").slice(-5).reverse();if(userMsgs.length)list.innerHTML=userMsgs.map(x=>`<div class="recent-item">${escapeHtml(x.content)}</div>`).join("");function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}});