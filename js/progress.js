document.addEventListener("DOMContentLoaded",()=>{document.getElementById("completed").textContent=progress().length;document.getElementById("saved").textContent=saved().length;document.getElementById("chats").textContent=chats().filter(x=>x.role==="user").length;const list=document.getElementById("list");const p=progress();if(p.length)list.innerHTML=p.map(id=>`<div class="recent-item">${id}</div>`).join("")})